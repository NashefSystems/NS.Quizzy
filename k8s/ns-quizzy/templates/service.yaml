apiVersion: v1
kind: Service
metadata:
  name: {{ include "ns-quizzy.fullname" . }}
  labels:
    {{- include "ns-quizzy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ns-quizzy.selectorLabels" . | nindent 4 }}
