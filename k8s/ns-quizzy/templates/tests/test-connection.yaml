apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ns-quizzy.fullname" . }}-test-connection"
  labels:
    {{- include "ns-quizzy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ns-quizzy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
